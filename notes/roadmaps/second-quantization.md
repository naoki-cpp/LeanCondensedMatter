# Roadmap — Second Quantization (Track D, feeds into Track A)

See [notes/roadmap.md](../roadmap.md) for the status table and how this track fits into the overall
plan. This track replaces the old "QFT groundwork" placeholder in
[quantum-theory-foundations.md](quantum-theory-foundations.md#basic-quantum-field-theory-formalization)
with a concrete, ordered construction, algebraic first — no Hilbert-space completion, closable
operators, or self-adjointness/spectral theory until the algebraic Linked Cluster Theorem is done.

## Dependency order

```
One-particle space (Mode, [Fintype Mode])
        ↓
Occupation-number basis (Mode →₀ ℕ)
        ↓
Bosonic Fock space (algebraic, finite-particle)
        ↓
Creation / annihilation operators
        ↓
CCR
        ↓
Hamiltonians (number operator, free, interaction)
        ↓
Gibbs state / partition function / free energy (reuse Track A/C infrastructure)
        ↓
Dyson/Duhamel expansion
        ↓
Linked Cluster Theorem (reuse Track B's mu_eq_prod_restrict)
```

Field operators `φ(x)` are NOT the starting point — they are introduced only after creation and
annihilation operators exist, as a later derived construction.

## Planned files, in build order

| # | File | Status |
|---|---|---|
| 1 | `OneParticleSpace.lean` — abstract `Mode : Type` with `[Fintype Mode]` initially (generalize to countably infinite later) | `proved` |
| 2 | `Occupation.lean` — `Occupation Mode := Mode →₀ ℕ`, the occupation-number basis | `proved` |
| 3 | `FockSpace.lean` — algebraic finite-particle vector space generated by occupation-number states. No completion, no bounded/unbounded operator theory yet | `idea` |
| 4 | `CreationAnnihilation.lean` — `create`/`annihilate` on basis vectors, extended linearly; vacuum, particle number, "creation raises / annihilation lowers occupation" proved before CCR | `idea` |
| 5 | `CCR.lean` — `[aᵢ, aⱼ] = 0`, `[aᵢ†, aⱼ†] = 0`, `[aᵢ, aⱼ†] = δᵢⱼ` | `idea` |
| 6 | `Hamiltonian.lean` — number operator, free Hamiltonian, interaction Hamiltonian as finite sums of creation/annihilation monomials. Finite mode sets still assumed | `idea` |
| 7 | `DysonExpansion.lean` — expand `exp(-βH)`, represent perturbative terms combinatorially. Gibbs state / partition function / free energy reuse existing Track A/C infrastructure — no duplicate definitions | `idea` |
| 8 | `QuantumLinkedCluster.lean` — apply Track B's `mu_eq_prod_restrict` (partition-lattice Möbius inversion) to show `log Z = Σ` over connected clusters. Reuse Track B directly; do not reprove | `idea` |

Track D's own aggregate status (as shown in `notes/roadmap.md`'s table) is `stated` once phase 1
lands, tracking the *earliest* non-`idea` phase — it moves to `proved` only once phase 8 is done.

## Fermionic case (CAR)

Deferred: bosonic case (CCR, `Occupation Mode := Mode →₀ ℕ`) comes first since it's the simpler
occupation-number representation to formalize; CAR (fermions, occupation in `Mode → Bool` or
`Mode →₀ Fin 2`-style, anticommutators) follows the same file order once the bosonic case is done.

## Relation to Track C (operator algebra)

Analytic issues — Hilbert-space completion of the algebraic Fock space, self-adjointness/domains of
the unbounded creation/annihilation operators, trace-class questions on the completed space — are
explicitly deferred past the algebraic Linked Cluster Theorem. When they're eventually needed,
Track C's Hilbert–Schmidt/trace-class infrastructure should be reused, not reproved.
