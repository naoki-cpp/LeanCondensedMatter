name: Component Pair Product Diagnostic

on:
  push:
    branches:
      - agent/component-pair-product
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: leanprover/lean-action@v1
        with:
          auto-config: false
      - name: Build target and capture output
        shell: bash
        run: |
          set +e
          # Keep this target-only build separate from the repository-wide CI.
          lake build +LeanCondensedMatter.SecondQuantization.Fermionic.Diagrammatics.WickDiagram.ComponentPairProduct \
            > component-pair-product-diagnostic.log 2>&1
          status=$?
          echo "exit_status=$status" >> component-pair-product-diagnostic.log
          exit 0
      - name: Upload diagnostic log
        uses: actions/upload-artifact@v4
        with:
          name: component-pair-product-diagnostic
          path: component-pair-product-diagnostic.log
          if-no-files-found: error
          retention-days: 1