name: Apply PR 452 Core import fix

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: write

concurrency:
  group: pr452-core-import-fix
  cancel-in-progress: true

jobs:
  apply:
    if: github.event.pull_request.number == 452 && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Add direct ordered-simplex import
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('LeanCondensedMatter/SecondQuantization/Fermionic/Diagrammatics/DysonDiagramExpansion/Core.lean')
          text = path.read_text(encoding='utf-8')
          anchor = 'import LeanCondensedMatter.SecondQuantization.Common.Diagrammatics.Ordered\n'
          addition = anchor + 'import LeanCondensedMatter.Analysis.OrderedSimplex.Integral\n'
          assert text.count(anchor) == 1
          assert 'import LeanCondensedMatter.Analysis.OrderedSimplex.Integral\n' not in text
          path.write_text(text.replace(anchor, addition, 1), encoding='utf-8')
          PY

      - name: Remove temporary workflow
        run: rm .github/workflows/apply-pr452-core-import-fix.yml

      - name: Commit fix
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Add direct ordered-simplex import to Dyson core"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
